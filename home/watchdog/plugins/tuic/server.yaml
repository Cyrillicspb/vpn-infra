# TUIC server-side runtime config template
# Реальный JSON генерируется install-vps.sh / deploy.sh в /opt/vpn/sing-box/tuic-server.json
listen_port: ${TUIC_PORT}
cert: /etc/sing-box/certs/server.crt
key: /etc/sing-box/certs/server.key

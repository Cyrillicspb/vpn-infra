# /etc/dnsmasq.d/vpn-force.conf
# Генерируется автоматически скриптом update-routes.py
# Домены из /etc/vpn-routes/manual-vpn.txt (добавленные через /vpn add)
# НЕ РЕДАКТИРОВАТЬ ВРУЧНУЮ
#
# Формат идентичен vpn-domains.conf:
#   server=/<domain>/<vps-dns>
#   nftset=/<domain>/4#inet#vpn#blocked_dynamic
#
# Отличие: manual-vpn.txt — управляемые вручную через Telegram-бот,
# vpn-domains.conf — генерируется из баз РКН/antifilter автоматически.
#
# VPS_DNS = 10.177.2.2

# (пусто при первом запуске, заполняется через /vpn add <домен>)

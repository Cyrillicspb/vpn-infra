# Telegram-бот requirements — пинированные версии

# aiogram 3.x — Telegram bot framework
aiogram==3.26.0

# HTTP клиент (watchdog API + notify server)
aiohttp==3.13.3

# Pydantic (aiogram dependency)
pydantic==2.7.1

# QR-код для конфигов (только если AllowedIPs <= 50)
qrcode[pil]==7.4.2
Pillow==11.1.0

# Jinja2 — шаблоны .conf файлов
Jinja2==3.1.6

# Системный мониторинг (опционально)
psutil==7.2.2
